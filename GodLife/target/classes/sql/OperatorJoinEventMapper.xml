<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper
		PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
		"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="OperatorJoinEventMapper">
 	
 	
	<resultMap id="operatorJoinEventSelectMap" type="operatorJoinEvent">
		<result property="joinEventNo" 	column="join_event_no" 	jdbcType="INTEGER"/>
		<result property="userEmail"	column="user_email" 	jdbcType="VARCHAR" />
		<result property="rewardNo" 	column="reward_no" 		jdbcType="INTEGER" />
		<result property="regDate" 		column="reg_date" 		jdbcType="DATE" />
	</resultMap>
	
	<!-- SQL : INSERT -->
	<insert 	id="addOperatorJoinEvent"		parameterType="operatorJoinEvent" >
	 	INSERT
		INTO join_event( join_event_no , user_email , reward_no , reg_date) 
		VALUES	 (	seq_events_event_no.NEXTVAL, #{userEmail:VARCHAR} , #{rewardNo:INTEGER} , SYSDATE )
	 </insert>
	 
	 <!-- SQL : SELECT ONE -->
	 <select 	id="getOperatorJoinEvents"	parameterType="int" resultMap="operatorJoinEventSelectMap">
		SELECT
		join_event_no , user_email , reward_no , reg_date
		FROM join_event 
		WHERE join_event_no = #{joinEventNo}
	 </select>
	 
	 <!-- SQL : UPDATE -->
	 <update	id="updateOperatorJoinEvent"	parameterType="operatorJoinEvent" >
	   	UPDATE join_event
	   	<set>
	   		join_event_no 	= 	#{rewardNo} 
	   	</set>
		WHERE event_no = #{joinEventNo}
	 </update>

	 <!-- SQL : DELETE -->
	 <delete	id="deleteOperatorJoinEvent"	parameterType="operatorJoinEvent" >
	   	DELETE join_event
		WHERE join_event_no = #{joinEventNo}
	 </delete>
		 
	<!-- SQL : SELECT LIST -->
	<select  id="getOperatorJoinEventList"  parameterType="search"	resultMap="operatorJoinEventSelectMap">
	  	SELECT *
	  	FROM (	SELECT inner_table.* , ROWNUM AS row_seq
	  					FROM		(	SELECT join_event_no , user_email , reward_no, reg_date
											FROM join_event
											<if test="searchCondition != null">
												<where>
													<if test="searchCondition == 0 and searchKeyword !='' ">
										 				join_event_no = #{searchKeyword}
													</if>
													<if test="searchCondition == 1 and searchKeyword !='' ">
										 				reg_date = #{searchKeyword}
													</if>
												</where>
											</if>
											ORDER BY join_event_no ) inner_table
						WHERE ROWNUM &lt;= #{endRowNum} )
		WHERE row_seq BETWEEN #{startRowNum} AND #{endRowNum} 
	 </select>
	 	<!--  위 두번째 subQuery 의  
	 			WHERE ROWNUM &lt;= #{endRowNum} ) 는
	 			WHERE ROWNUM <= #{endRowNum} ) 의미이며..
	 			< 는 keyword 로 &lt; 를	사용.
	 			
	 			<![CDATA[  ~~~  ]]> 를 이용하여 아래와 같이 사용  할 수 있다.
	 			CDATA ==> Character Data 의 의미 Java 에서 \n 같은 특수문자 처리  
				WHERE ROWNUM <![CDATA[ <=]]> #{endRowNum} )
		-->
	
	<!-- SQL : SELECT ROW Count -->	 
	 <select  id="getOperatorJoinEventTotalCount"  parameterType="search"	 resultType="int">
	  	SELECT COUNT(*)
	  	FROM(	SELECT join_event_no , user_email , reg_date
						FROM join_event
						<if test="searchCondition != null">
							<where>
								<if test="searchCondition == 0 and searchKeyword !='' ">
						 			join_event_no = #{searchKeyword}
								</if>
								<if test="searchCondition == 1 and searchKeyword !='' ">
						 			reg_date = #{searchKeyword}
								</if>
							</where>
						</if> ) countTable						
	 </select>
	 
</mapper>